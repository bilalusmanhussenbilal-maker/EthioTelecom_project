# Render/Vercel builds run with NODE_ENV=production, which makes npm skip
# devDependencies by default. The server build needs typescript, prisma and the
# @types packages, so force dev dependencies to be installed.
include=dev
